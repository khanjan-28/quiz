deploy code from local to github repo which is already created

commands with one-line descriptions:

cd D:\dev\your-project

👉 Move into your project folder

git init

👉 Initialize Git in your project

git add .

👉 Stage all files for commit

git commit -m "Initial commit"

👉 Save snapshot of your code locally

git remote add origin git@github.com:username/repo.git

👉 Link local repo to GitHub using SSH

git branch -m master main

👉 Rename branch from master to main

git push -u origin main

👉 Push code to GitHub and set upstream branch




git add .

👉 Use when you have changed files and want Git to track those changes

git commit -m "your message"

👉 Use when you want to save those changes locally with a message

git push

👉 Use when you want to upload your committed changes to GitHub




git fetch

👉 Download latest changes from GitHub without modifying your local files

git pull

👉 Download and apply (merge) latest changes into your local code
---------------------------------------
